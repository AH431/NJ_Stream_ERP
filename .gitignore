資料版本迭代/
.claude/

# Dependencies
node_modules/
# package-lock.json 納入版控（npm ci 需要它，供應鏈安全防護）

# Environment（含密碼、JWT Secret 等）
.env
.env.local
.env.*.local
.env.development
.env.production
**/.env
**/.env.local
!**/.env.example

# Build output
dist/

# Drizzle migrations — 納入版控（提供審計軌跡，deploy 依賴 db:migrate 流程）
# db:push 已從 package.json 移除，禁止繞過 migration 直接推 schema

# 敏感設定檔（含 DB 密碼、腳本等）
# docker-compose.prod.yml 不在此列 — 生產設定檔納入版控（secrets 來自 .env.production）
docker-compose.yml
docker-compose.override.yml
docker-compose.local.yml
setup_gh_tasks.sh

# 生產環境變數（含密碼，不進版控）
.env.production

# 系統 / IDE
.DS_Store
Thumbs.db
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore

# Android release signing（key.properties.example 納入版控，實際值不進版控）
packages/frontend/android/key.properties

# Firebase（google-services.json.example 納入版控，實際值不進版控）
packages/frontend/android/app/google-services.json
keystores/

# APK debug symbols（含原始碼對應，勿公開）
packages/frontend/build/debug-info/

# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies

# Drift / Code generation
# *.g.dart  ← 暫時 commit generated files；決定不 commit 時再取消註解

# Android / iOS build
android/.gradle/
android/app/build/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/Flutter/Generated.xcconfig
ios/Runner.xcworkspace/
ios/build/
ios/.symlinks/

# 日誌與快取
*.log
logs/
LOG/
cache/
tmp/

# 測試 / 本地 SQLite DB（避免把本機資料 push 上去）
**/*.sqlite
**/*.db

# 暫存 scratch 實驗腳本（不應進版控）
scratch_*.ts
scratch_*.dart

# 暫存分析輸出（不應進版控）
packages/frontend/*.txt

# 產出物（PDF / 說明書，binary 輸出不進版控）
*.pdf
NJ_Stream_ERP_Brochure.pdf

# 二進位架構文件（docx / xlsx，無法 diff，改用 markdown 維護）
*.docx
*.xlsx
*.pptx

# 敏感架構文件（API contract、ERD，若 repo 轉公開需先評估）
docs/api-contract-*.yaml
docs/nj_stream_erp_erd.html

# WAF / 資安設定細節（含 Cloudflare 規則，勿公開）
docs/cloudflare-waf-setup.md

# AI Service — ChromaDB 向量資料庫（二進位，不進版控）
packages/ai_service/db/
# AI Service — Python 虛擬環境
packages/ai_service/venv/
packages/ai_service/.venv/
# AI Service — Python cache
packages/ai_service/**/__pycache__/
packages/ai_service/**/*.pyc

# Phase 3 PRD 草稿（v1–v4 為迭代中版本，僅保留最終版 v5）
docs/NJ_Stream_ERP Phase3 PRD v1.0.md
docs/NJ_Stream_ERP Phase3 PRD V2_codex.md
docs/NJ_Stream_ERP Phase3 PRD v3.0.md
docs/NJ_Stream_ERP Phase3 PRD V4_codex.md

# Editor
.idea/
.vscode/
*.iml
