# 工作日誌 — 2026-04-14

**Sprint**：W6–W8 SCM  
**分支**：main  

---

## Issue #14 — W6 末端到端走路測試 ✅

### 環境建置（一次性）

| 項目 | 結果 |
|------|:----:|
| Android Command-line tools 安裝 + ANDROID_HOME 設定 | ✅ |
| JDK 17（Eclipse Temurin 17.0.18）安裝 + JAVA_HOME | ✅ |
| `sdkmanager` 安裝 `platforms;android-33` + `build-tools;33.0.2` + `ndk;27.0.12077973` | ✅ |
| `flutter doctor --android-licenses` 授權接受 | ✅ |
| `flutter config --jdk-dir` 指向 JDK 17 | ✅ |
| `flutter create --platforms=android .` 產生 android/ | ✅ |
| `android/app/build.gradle.kts` minSdk 改為 23 | ✅ |
| `ndkVersion = "27.0.12077973"` 修正（flutter_secure_storage 需求）| ✅ |
| Sony XA1（RQ3006EUNZ）ADB 連線 + flutter run 部署 | ✅ |

**環境建置備註**：
- Android SDK 路徑：`C:\Users\archi\OneDrive\Documents\軟體_驅動程式`
- 舊 Java 11（Oracle）會被 PATH 優先抓到，需透過 `flutter config --jdk-dir` 明確指定 JDK 17
- `platform-tools.backup` 重複路徑警告為良性，不影響功能

---

### 走路測試流程

#### Phase 0：環境準備

| 步驟 | 結果 |
|------|:----:|
| 後端啟動（`npm run dev`，`:3000`）| ✅ |
| JWT 取得（admin_test）| ✅ |
| 庫存確認（productId=3, onHand=12, reserved=0）| ✅ |
| reserved=8 舊殘留 → curl cancel 清零 | ✅ |
| Sony XA1 App 部署 + 登入畫面 | ✅ |

#### Phase 1：離線建單

| 步驟 | 結果 |
|------|:----:|
| 初始 Pull（庫存、產品資料同步至本地）| ✅ |
| 飛航模式開啟 | ✅ |
| 建立客戶「走路測試客戶」（本地 id 橘色雲朵）| ✅ |
| 建立報價（TST-I7-001 × 3，含稅 5%）| ✅ |

**注意**：報價轉訂單需要報價已同步（`q.id > 0`），流程調整為先 Push 再轉單。

#### Phase 2：同步 Push

| 步驟 | 結果 |
|------|:----:|
| 關閉飛航模式 → 點雲朵上傳推送 | ✅ |
| 報價 id=3（走路測試客戶）→ `converted`，訂單 id=4 建立 | ✅ |
| 伺服器確認：客戶 id=4、報價 id=3、訂單 id=4（status=pending）| ✅ |

#### Phase 3：確認訂單 + 預留庫存

| 步驟 | 結果 |
|------|:----:|
| 確認訂單（AlertDialog → confirmed）| ✅ |
| ReserveInventoryDialog：預留數量 3，可出貨 12，無警示 | ✅ |
| Push → 伺服器：訂單 confirmed，reserved: 0→3 | ✅ |

#### Phase 4：出貨（admin_test）

| 步驟 | 結果 |
|------|:----:|
| 切換 admin_test 登入 | ✅ |
| Tab 5 Pull-to-refresh（更新本地 reserved=3 快照）| ✅ |
| ShipOrderDialog：出貨 3，onHand 12→9，reserved 3→0，無警示 | ✅ |
| Push → 伺服器：訂單 shipped，onHand=9，reserved=0 | ✅ |

#### Phase 5：最終一致性

| 步驟 | 結果 |
|------|:----:|
| App Tab 5 Pull-to-refresh | ✅ |
| 本地顯示 onHand=9，reserved=0 | ✅ |

---

### 最終庫存驗收

| 時間點 | quantityOnHand | quantityReserved |
|--------|:--------------:|:----------------:|
| 初始   | 12 | 0 |
| reserve 後 | 12 | 3 |
| out 後（最終）| **9** | **0** |

---

### 過程中發現並修正的問題

#### Bug 1：空列表 RefreshIndicator 無法觸發

**問題**：`inventory_list_screen.dart` 與 `sales_order_list_screen.dart` 在列表空白時
顯示 `Center` 文字，`RefreshIndicator` 不在畫面上，下拉手勢無效。

**修正**：
- 將 `RefreshIndicator` 移到 empty check 外層
- 空狀態改用 `ListView`（含 `AlwaysScrollableScrollPhysics`）包裝，確保下拉永遠有效
- 修改檔案：`inventory_list_screen.dart`、`sales_order_list_screen.dart`

#### Bug 3：sales_order:create FORBIDDEN_OPERATION 後本地記錄未回滾

**問題**：報價轉訂單被伺服器拒絕（FORBIDDEN_OPERATION，報價已被轉換）時，
本地 `sales_orders` 表中樂觀寫入的記錄未清除，導致訂單列表出現無法操作的
「待處理」殘留訂單（id < 0，無取消按鈕，因 `canCancel` 要求 `!isOffline`）。

**重現路徑**：離線建立報價 → 同步 → 誤觸已轉換報價的「轉訂單」→ FORBIDDEN_OPERATION → 殘留。

**修正方向**：`_handleError` 中對 `sales_order:create` 的 FORBIDDEN_OPERATION
應額外呼叫 `db.deleteSalesOrder(localId)`（軟刪除，設 deletedAt），
讓 `watchActiveSalesOrders()` 自動過濾掉殘留記錄。

---

#### Bug 2：ShipOrderDialog 出貨前需手動 Pull

**問題**：reserve Push 後，本地庫存快照未自動更新，ShipOrderDialog 讀到 reserved=0
而非 reserved=3，顯示 ⚠️ 警示「尚未預留，服務端將拒絕」。

**暫行方案**：出貨前手動 Pull-to-refresh（Tab 5）更新本地快照。

**待辦**：在 `_shipOrder()` 或 ShipOrderDialog 開啟前自動呼叫 `pullData()`，
或加入提示「建議出貨前先同步以取得最新庫存狀態」。

---

### 走路測試結論

**業務閉環完整走通**：客戶 → 報價 → 訂單 → 確認 → 預留 → 出貨 ✅

Issue #14 所有核心 Phase 通過，W6 走路測試完成。

---

---

## UI 改善（同日）

### FAB 圖示統一

**問題**：客戶 FAB 使用 `person_add_outlined`（單一 24dp 設計圖示），產品和報價 FAB
只有 `Icons.add`，視覺不一致。

**過程**：嘗試 Stack 疊加與 Row 並排兩種方案，均因兩個獨立圖示的線重無法對齊
單一設計圖示而放棄，最終改用內建含 + 的 Material 圖示。

**修正**：

| Tab | 舊圖示 | 新圖示 |
|-----|--------|--------|
| 客戶 | `person_add_outlined` | 不變 |
| 產品 | `Icons.add` | `library_add_outlined` |
| 報價 | `Icons.add` | `note_add_outlined` |
| 庫存入庫 | `add_box_outlined` | 不變 |

**設計決策**：FAB 一律使用 Material 單一設計圖示，不拼接兩個圖示，確保線重一致。

### UI 設計規範確立

使用者回饋確立以下原則，適用所有後續 feature screen 實作：

- **標籤 / 狀態**：純文字 + 色彩 + 圖示（`Row(Icon + Text)`），無外框無背景
- **按鈕**：`TextButton.icon` / `OutlinedButton` / `FilledButton`，有外框或填色
- **整體**：專注 visual hierarchy and consistency

---

---

## 程式碼稽核（同日）

全面檢驗專案檔案完整性、內容合規與跨階段一致性，發現並修正 3 個問題：

### Fix 1：`updateSalesOrderStatus` 清除審計時間戳

**問題**：`Value(confirmedAt)` 在未傳入時為 `Value(null)`，取消訂單時會清除 `confirmedAt`。  
**修正**：`sales_order_dao.dart` — 改為 `confirmedAt != null ? Value(confirmedAt) : const Value.absent()`

### Fix 2：`inventoryToState()` entityType 字串錯誤

**問題**：`sync.service.ts` 的 `inventoryToState()` 回傳 `entityType: 'inventory_delta'`，與 Pull route 的 `'inventory_item'` 不一致。  
**修正**：`sync.service.ts` + `payloads.ts` — 統一改為 `'inventory_item'`

### Fix 3：inventory items Pull response 缺 `deletedAt`

**問題**：`sync.route.ts` inventory items 回應缺少 `deletedAt` 欄位。  
**修正**：補上 `deletedAt: r.deletedAt ? r.deletedAt.toISOString() : null`

### 稽核確認已完成（日誌標記待辦但程式碼已完成）

| 項目 | 狀態 |
|------|:----:|
| Bug 2：ShipOrderDialog 出貨前自動 Pull | ✅ `sales_order_list_screen.dart:343` |
| Bug 3：FORBIDDEN_OPERATION 回滾本地訂單 | ✅ `sync_provider.dart:582` |

---

## UI 規範對齊（同日）

將報價列表與訂單列表依確立的 UI 設計規範全面對齊。

### 訂單列表（`sales_order_list_screen.dart`）

- `_buildStatusChip()` → `_buildStatusLabel()`：`Chip(backgroundColor)` 改為 `Row(Icon + Text)`
- 補 2 處 `context.mounted` 防護（`showDialog` await 後的 `context.read`）

| status | icon | color |
|--------|------|-------|
| `pending` | `schedule` | grey |
| `confirmed` | `check_circle_outline` | blue |
| `shipped` | `local_shipping_outlined` | green |
| `cancelled` | `cancel_outlined` | red |

### 報價列表（`quotation_list_screen.dart`）全面重構

| 變更項目 | 舊 | 新 |
|---------|----|----|
| 狀態標籤 | `Chip(backgroundColor)` | `Row(Icon + Text)` |
| 佈局 | `ListTile` | `Card + Column`（對齊訂單列表）|
| 操作按鈕 | `IconButton`（純圖示）| `TextButton.icon`（含文字）|
| 資訊列 | 僅合計金額 | 合計金額 + 建立日期 |

---

---

## Issue #15 — 簡易儀表板實作 ✅

### 實作範疇

PRD §4.3 三項指標，全部來自本地 Drift 查詢（StreamBuilder），離線可用：

1. **待出貨訂單數**（`status = confirmed`）
2. **低庫存警示列表**（`available <= minStockLevel`，`minStockLevel > 0`）
3. **本月報價總額**（含稅，`createdAt >= 本月第一天`）

### 新增 DAO 方法

| 方法 | 位置 |
|------|------|
| `watchConfirmedOrderCount()` | `sales_order_dao.dart` |
| `watchCurrentMonthQuotationTotal()` | `quotation_dao.dart` |
| `watchLowStockItems()` + `LowStockItem` class | `inventory_items_dao.dart` |

### 新增 UI

`features/dashboard/dashboard_screen.dart` 新建：
- `_ConfirmedOrderCard`：待出貨訂單數，有訂單時 icon 橘色，清空時綠色
- `_MonthlyQuotationCard`：本月報價總額，整數部分加千分位格式化
- `_LowStockSection`：低庫存警示列表，含品名 / SKU / 可出貨量 / 安全庫存 / 缺貨量
- `_SummaryCard`：共用大數字摘要 Card widget

### main.dart 更新

- Dashboard 插入 Tab 0（儀表板），原有 5 個 Tab 後移至 index 1–5
- FAB index 判斷全部 +1（客戶 0→1、產品 1→2、報價 2→3、庫存 4→5）
- 移除未使用的 `_addBadgeIcon` helper
- 新增 `dashboard_screen.dart` import

### 過程中修正

**`quotation_dao.dart` TypeConverter 問題**：`isBiggerOrEqualValue(DateTime)` 在 Drift mapped TextColumn 中需傳入 ISO 字串而非 DateTime；改為 `firstDayOfMonth.toIso8601String()` 後 `dart analyze` 通過。

`dart analyze` 驗收：0 issues（5 個修改檔案全數通過）。

---

## Contract 稽核 — Issue #19 + #22 修正

### Issue #19：ServerState discriminator 修正

**問題 1**：`ServerState.discriminator.mapping` 的鍵寫成 `inventory_delta`，但後端 `inventoryToState()` 實際返回 `entityType: 'inventory_item'`，兩者不一致。  
**修正**：mapping 改為 `inventory_item: '#/components/schemas/InventoryItemPayload'`

**問題 2**：原 Dart 範例寫 `failedOp.entityType`，但 `FailedOperation` schema 沒有此欄位；且未處理 `server_state = null`（`INSUFFICIENT_STOCK`）的情境。  
**修正**：改為 `serverState['entityType']`，並在 switch 前加 null guard。

### Issue #22：GET /api/v1/sync/pull 端點凍結

**背景**：合約 yaml 的 pull 端點長期標注 TBD；回傳 schema key `inventoryDeltas` 與 backend/frontend 實作的 `inventoryItems` 不符。

**修正項目**：

| 項目 | 舊 | 新 |
|------|----|----|
| 端點狀態 | TBD（未來實作）| FINAL — W6 |
| 回傳 key | `inventoryDeltas` | `inventoryItems`（新增 `SyncPullResponse` schema）|
| pull 端點說明 | 無 | 三大用途 + 前端處理流程 + 分頁說明 |
| `SalesOrderPayload` | 缺 `items` 欄位 | 補上（backend pull 實際有回傳）|

### 待手動關閉

- Issue #21（`api-contract-auth.yaml` 已凍結 FINAL v1.0）
- Issue #20（users 不納入 sync entityType，決策已落地）

---

## 下次待辦

| 優先 | 項目 |
|------|------|
| 🟡 | Issue #19 / #22 GitHub 關閉（手動）|
| ⬜ | Phase 6 邊界情境補測（時間允許）|
