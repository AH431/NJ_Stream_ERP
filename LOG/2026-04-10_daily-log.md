# 工作日誌 — 2026-04-10

**Sprint**：W1–W2 Foundation  
**分支**：master（→ origin/main）  
**最新 commit**：`50b630c`

---

## 今日完成事項

### 1. Issue #1 ✅ — OpenAPI Contract-First SSOT
- 產出 `docs/api-contract-sync-v1.6.yaml`（完整 OpenAPI 3.1.0 同步協定規格）
- `ServerState` 加入 discriminator（`entityType` 作為識別欄位）
- 新增 `externalDocs` 連結至 `api-contract-auth.yaml`
- 驗收清單寫入 `LOG/W1-W2_Issue1_A0_api-contract-sync-v1.6-checklist.md`
- **Issue #1 已關閉**

### 2. Issue #2 — Agent A-1：Fastify + Drizzle Schema（全 8 表）
- 初始化 `packages/backend/`（Fastify v5 + Drizzle ORM + PostgreSQL）
- 完成全 8 張資料表 schema：
  - `users`, `customers`, `products`, `quotations`, `sales_orders`
  - `order_items`（取代原 warehouses，正規化訂單明細）
  - `inventory_items`（含 DB CHECK constraints）
  - `processed_operations`（含 UNIQUE + 2 indexes）
- 修正 drizzle-kit ESM/CJS 相容性問題（`.ts` 副檔名 + `allowImportingTsExtensions`）
- Relations 集中於 `schemas/index.ts`（避免循環 import）
- 驗收清單寫入 `LOG/W1-W2_Issue2_A1_fastify-drizzle-schema-checklist.md`
- **W1 Gate 待執行**（需 Docker，明日繼續）

### 3. Auth API YAML（Issue #21 進行中）
- 產出 `docs/api-contract-auth.yaml` DRAFT
- 涵蓋：POST /login、/refresh、/logout
- JWT Payload：`{ userId, role, exp }`；Access 1h / Refresh 30d
- Role enum：`sales` / `warehouse` / `admin`

### 4. 資安處置 ✅ — `.env` 歷史清除
- 發現 `packages/backend/.env`（含明文 DB 密碼與 JWT Secret）曾進入 git 歷史
- 執行 `git-filter-repo` 完整清除全 17 筆 commit 中的 `.env`
- Force push 至 GitHub，本地 reset 同步
- 憑證全數輪換（PostgreSQL 密碼 + JWT Secret → 隨機高強度值）
- `.gitignore` 強化（`**/.env` glob、`docker-compose.yml`、`*.pem` 等）
- 完整記錄寫入 `LOG/W1-W2_Security_env-history-purge.md`

---

## 明日待辦

| 優先 | 任務 | Issue |
|------|------|-------|
| 🔴 高 | 安裝 Docker Desktop，執行 W1 Gate 驗收（`db:push` + `db:studio`） | #2 |
| 🟡 中 | Agent B-1：Flutter + Drift Schema + build_runner | #3 |
| 🟡 中 | 凍結 `api-contract-auth.yaml`，關閉 Issue #21 | #21 |

### W1 Gate 指令（Docker 啟動後直接執行）
```bash
# 1. 啟動資料庫
docker compose up -d

# 2. 推送 schema
cd packages/backend
npm run db:push

# 3. 目視確認全 8 張表
npm run db:studio
# → 開啟瀏覽器，確認 users / customers / products /
#   quotations / sales_orders / order_items /
#   inventory_items / processed_operations 均存在
```
確認後關閉 Issue #2，進入 W2 階段。

---

## 今日 Commit 記錄

| Hash | 訊息 |
|------|------|
| `50b630c` | docs(LOG): 記錄 .env 歷史清除完整流程與憑證輪換結果 |
| `128a8b5` | security: 移除 .env 追蹤並強化 .gitignore |
| `c973fdd` | docs(LOG): 更新 Issue #2 驗收清單（含問題診斷與修正記錄） |
| `a1d2be6` | fix(W1-W2): 修正 drizzle-kit ESM/CJS 相容性問題 |
| `450d512` | feat(W1-W2/Issue#2): Agent A-1 Fastify + Drizzle Schema 全 8 表 |
| `ea1e986` | security: 移除敏感設定檔並加入 .gitignore |
| `c383fea` | feat(W1-W2): Contract-First SSOT — Sync & Auth API 規格產出 |
