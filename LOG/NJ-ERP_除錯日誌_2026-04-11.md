# NJ-ERP 環境建置除錯日誌

**日期**：2026-04-11  
**專案路徑**：`C:\Projects\NJ_Stream_ERP`

---

## 問題排除時間軸

### Phase 1：Docker Engine 無法啟動

| 類型 | 內容 |
|------|------|
| ERROR | Docker CLI 無法辨識（CommandNotFoundException） |
| 症狀 | `docker compose`、`docker ps` 完全無法執行 |
| DIAG | Docker Desktop v4.68.0 已安裝，但 Engine 卡在 `starting` 狀態（RAM 0.00 GB） |
| ROOT | WSL 為舊版 inbox 架構，不支援 WSL 2，`wsl --version` 輸出 help page 而非版本號 |
| FIX | 執行 `wsl --install --web-download` 升級 WSL |
| 結果 | WSL 2.6.3.0 · Kernel 6.6.87.2-1 · WSLg 1.0.71 |
| PASS | 重啟 Docker Desktop → Engine running（綠色）· RAM 2.45 GB · Server Version: 29.3.1 |

---

### Phase 2：啟動 PostgreSQL 容器

| 類型 | 內容 |
|------|------|
| 指令 | `docker compose up -d nj-erp-postgres` |
| PASS | Container `nj-erp-postgres` Started |
| 詳情 | Image: `postgres:16-alpine` · Network: `nj_stream_erp_default` · Volume: `nj_stream_erp_postgres_data` |

---

### Phase 3：pnpm 與 PowerShell 執行政策

| 類型 | 內容 |
|------|------|
| ERROR | `pnpm` CommandNotFoundException |
| ERROR | `npx` PSSecurityException（執行政策 Restricted） |
| FIX | `Set-ExecutionPolicy RemoteSigned -Scope CurrentUser` |
| FIX | `npm install -g pnpm` → pnpm 10.33.0 |

---

### Phase 4：W1 Gate 驗收

| 指令 | 結果 |
|------|------|
| `pnpm install` | Done in 16.1s |
| `npx tsc --noEmit` | 0 errors |
| `pnpm run db:generate` | 8 tables 偵測，schema 已是最新 |
| `pnpm run db:push` | Changes applied |
| `pnpm run db:studio` | Running on https://local.drizzle.studio |

**建立的資料表（8 張）：**

`customers` · `inventory_items` · `order_items` · `processed_operations` · `products` · `quotations` · `sales_orders` · `users`

---

## 問題根因彙整

| 問題 | 根本原因 | 解法 |
|------|----------|------|
| Docker Engine 無法啟動 | WSL 為舊版 inbox（WSL 1），不支援 Docker backend | `wsl --install --web-download` → WSL 2.6.3.0 |
| pnpm 無法辨識 | Node.js 已安裝但未全域安裝 pnpm | `npm install -g pnpm` |
| npx 執行被封鎖 | PowerShell 執行政策預設 Restricted | `Set-ExecutionPolicy RemoteSigned` |
| `docker ps \| grep` 語法錯誤 | grep 為 Unix 指令，PowerShell 不支援 | 改用 `Select-String` 或 `--filter` 參數 |

---

## 最終結論

**W1 Gate 驗收完成，環境建置成功。**  
Drizzle Studio 運行於 `https://local.drizzle.studio`，可進入下一 Agent 任務。

---

## W1 Gate 後續 — Issue #21 關閉

### Phase 5：凍結 api-contract-auth.yaml（Issue #21）

| 項目 | 內容 |
|------|------|
| 目標 | 將 Auth API Contract 由 DRAFT 升為 FROZEN，關閉 Issue #21 |
| 對照依據 | `users` schema、`USER_ROLES` 常數、`api-contract-sync-v1.6.yaml` |

**一致性驗證結果：**

| 合約欄位 | 對應 Schema/常數 | 狀態 |
|----------|-----------------|------|
| `LoginRequest.username` | `users.username varchar(100)` | ✅ |
| `TokenResponse.role` enum | `USER_ROLES: sales/warehouse/admin` | ✅ |
| `403 ACCOUNT_DISABLED` | `users.isActive boolean` | ✅ |
| JWT `userId / role` | Sync API 合約描述 | ✅ |

**補充修改（凍結前邏輯缺口）：**

- `/api/v1/auth/refresh` 補 `403 ACCOUNT_DISABLED`  
  原因：Refresh Token 尚有效但 `isActive = false` 時，後端應拒絕刷新；原合約僅有 `401`，無法涵蓋此狀況。

**最終狀態：**

| 文件 | 版本 | 狀態 |
|------|------|------|
| `docs/api-contract-auth.yaml` | v1.0 | FROZEN — 2026-04-11 |

Commit: `docs(auth): 凍結 api-contract-auth.yaml v1.0`  
Issue #21 — CLOSED

---

## W2 實作 — Issue #23 Auth Route Handler

### Phase 6：Schema 擴充與套件安裝

| 項目 | 內容 |
|------|------|
| Schema 變更 | `users` 表新增 `refresh_token varchar(512)`（可為 null，一人一 session） |
| Migration | `drizzle-kit generate` → `0001_cooing_stark_industries.sql` |
| DB Push | `ALTER TABLE "users" ADD COLUMN "refresh_token" varchar(512);` — Applied |
| 套件安裝 | `jsonwebtoken 9.0.3` + `bcryptjs 3.0.3`（純 JS，Windows 免 native binding） |

### Phase 7：AuthService + Route Handler 實作

| 檔案 | 功能 |
|------|------|
| `src/types/auth.ts` | JwtPayload / TokenResponse / AuthErrorResponse 型別 |
| `src/plugins/db.ts` | export `DrizzleDb` 型別供 Service 層使用 |
| `src/services/auth.service.ts` | login / refresh / logout 業務邏輯 |
| `src/routes/auth.route.ts` | Fastify 路由 + Zod 請求驗證 |
| `src/app.ts` | 掛載 `authRoutes` 至 `/api/v1/auth` |

**設計決策記錄：**
- JWT Payload: `{ userId: number, role: string }`（userId 為 integer，小寫 role），與合約一致
- Refresh Token 儲存：`users.refreshToken` 欄位（MVP 單一 session，簡單可靠）
- bcryptjs 優先於 bcrypt：Windows 環境免 node-gyp 編譯問題

**型別檢查：** `tsc --noEmit` → 0 errors

### Phase 8：驗收測試結果（curl）

| # | 測試案例 | 端點 | 期望 | 結果 |
|---|----------|------|------|------|
| AC1 | 登入成功，回傳 Token 組合 | POST /login | 200 + accessToken/refreshToken/role/userId | ✅ |
| AC2 | 密碼錯誤 | POST /login | 401 INVALID_CREDENTIALS | ✅ |
| AC3 | 帳號停用 | POST /login | 403 ACCOUNT_DISABLED | ✅ |
| AC4 | Refresh 成功，回傳新 Access Token | POST /refresh | 200 + accessToken | ✅ |
| AC5 | Refresh 時帳號已停用 | POST /refresh | 403 ACCOUNT_DISABLED | ✅（由 AC3 涵蓋，停用帳號無法取得 refreshToken） |
| AC6 | Refresh Token 無效 | POST /refresh | 401 REFRESH_TOKEN_EXPIRED | ✅ |
| AC7 | Logout 後 Refresh Token 失效 | POST /logout → POST /refresh | 204 → 401 | ✅ |

**JWT Payload 驗證（decode AC1 accessToken）：**
```json
{ "userId": 1, "role": "sales", "iat": 1775883848, "exp": 1775887448 }
```
- `exp - iat = 3600`（1 小時）✅
- `role` 小寫 ✅，`userId` integer ✅

Commit: `feat(backend): Issue #23 Auth Route Handler 實作`  
Issue #23 — CLOSED
