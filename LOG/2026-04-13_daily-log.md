# 工作日誌 — 2026-04-13

**Sprint**：W6–W8 SCM  
**分支**：main  

---

## Issue #9 — SCM 基礎建設：訂單列表 + 確認訂單 → reserve ✅

**Milestone**：W6–W8 SCM

---

### 前置 — Agent A：後端 GET /pull 補 inventoryItems

**修改**：`packages/backend/src/routes/sync.route.ts`

| 變更 | 說明 |
|------|------|
| Import `inventoryItems` schema | 補齊原本缺漏的 import |
| `inventoryDeltas: []` → `inventoryItems: []` | key 名對齊前端解析邏輯 |
| 新增 `inventory_delta` 增量查詢 | `WHERE updated_at > sinceDate`，回傳 entityType: `'inventory_item'` |

**驗收**：`npm run build` 通過（TypeScript 0 錯誤）

---

### Phase 0：前端 Schema Migration（schemaVersion 3）

**修改**：`packages/frontend/lib/database/schema.dart`  
**修改**：`packages/frontend/lib/database/database.dart`

- `InventoryItems` table 補 `minStockLevel` 欄位（對應後端 `min_stock_level`，default 0）
- `schemaVersion: 2 → 3`
- `onUpgrade from < 3`：`await m.addColumn(inventoryItems, inventoryItems.minStockLevel)`
- `build_runner build` 成功（41 outputs，`InventoryItemsCompanion` 含 `minStockLevel`）

**背景**：後端 `inventory_items` 表有 `min_stock_level` 欄位，前端原本缺漏，導致 Pull 後無法儲存低庫存警示閾值。

---

### Phase 1：InventoryItemsDao（新增）

**新增**：`lib/database/dao/inventory_items_dao.dart`

| 方法 | 說明 |
|------|------|
| `watchInventoryItems()` | `SELECT * ORDER BY productId ASC`，回傳 Stream |
| `getInventoryItemByProductId(int productId)` | 單筆查詢，供確認訂單計算 reserve |
| `upsertInventoryItemFromServer(companion)` | LWW upsert（同 CustomerDao 模式，依 updatedAt 判斷）|

---

### Phase 2：SalesOrderDao 擴充

**修改**：`lib/database/dao/sales_order_dao.dart`

| 新增方法 | 說明 |
|---------|------|
| `watchActiveSalesOrders()` | `WHERE deleted_at IS NULL ORDER BY updatedAt DESC`，Stream |
| `updateSalesOrderStatus(id, status, {confirmedAt, shippedAt})` | 樂觀更新 status + updatedAt |

---

### Phase 3：SyncProvider 擴充

**修改**：`lib/providers/sync_provider.dart`

| 變更 | 說明 |
|------|------|
| 新增 `enqueueDeltaUpdate(entityType, deltaType, payload)` | operationType: 'delta_update'，deltaType: reserve/cancel/out/in |
| `pullData()` entityTypes 加 `inventory_delta` | 解析 `data['inventoryItems']` → `_applyForceOverwrite('inventory_item', ...)` |
| `_applyForceOverwrite` 補 `inventory_item` case | `db.upsertInventoryItemFromServer(InventoryItemsCompanion(...))` |
| `INSUFFICIENT_STOCK` → Fail-to-Pull | `unawaited(pullData())`（非阻塞，同步協定 v1.6 §6）|
| 移除 unused import `schema.dart` | `dart analyze` 警告修正 |

---

### Phase 4：SalesOrderListScreen（新增）

**新增**：`lib/features/sales_orders/sales_order_list_screen.dart`

| 功能 | 實作 |
|------|------|
| 訂單列表 | `StreamBuilder<List<SalesOrder>>` 監聽 `watchActiveSalesOrders()` |
| 客戶名 | `initState` 一次載入 `customerMap`，避免 N+1 |
| 狀態 Chip | pending（灰）/ confirmed（藍）/ shipped（綠）/ cancelled（紅）|
| 來源標記 | `quotationId != null` → "報價轉入 #N" badge（橘色）|
| 離線 icon | `id < 0` → `cloud_upload_outlined`（橘）|
| 確認訂單 | sales/admin、pending、已同步、有 quotationId → 解析 quotation.items → enqueue reserve × N items + update confirmed |
| 取消訂單 | sales/admin、pending/confirmed、已同步 → AlertDialog 確認 → enqueue cancel delta（若原為 confirmed）|
| Pull-to-refresh | `RefreshIndicator` → `sync.pullData()` |

**確認訂單流程**：

```
quotation.items(JSON) → QuotationItemModel list
→ db.updateSalesOrderStatus(id, 'confirmed', confirmedAt: now)（樂觀）
→ sync.enqueueUpdate('sales_order', id, {status, confirmedAt, updatedAt})
→ for each item: sync.enqueueDeltaUpdate('inventory_delta', 'reserve', {productId, amount})
```

---

### Phase 5：HomeScreen 更新

**修改**：`lib/main.dart`

- `_titles` 加入 `'訂單管理'`（index 3）
- `IndexedStack` 加入 `SalesOrderListScreen()`
- `NavigationBar` 加入第四 destination（`Icons.shopping_bag_outlined` / label `'訂單'`）
- import `features/sales_orders/sales_order_list_screen.dart`
- Tab 3 無 FAB（訂單只能從報價轉入，不可直接新增）

---

### 驗收結果

| 驗收項目 | 結果 |
|---------|:----:|
| `dart analyze`（main.dart + sync_provider.dart + 新增三個檔案）| ✅ 0 issues |
| `dart analyze`（database.dart entry point）| ✅ 0 issues |
| `build_runner build`（schemaVersion 3）| ✅ 41 outputs |
| `InventoryItemsCompanion` 含 `minStockLevel` | ✅ 確認於 database.g.dart:666 |
| `npm run build`（後端）| ✅ 0 TypeScript 錯誤 |

---

---

## 確認事項：DELTA_UPDATE 四種 type 實作狀態

**結論：Issue #5 已完整實作，無須補做。**

### 驗證來源

`packages/backend/src/services/sync.service.ts` — `processInventoryDelta`

| 驗證項目 | 位置 | 狀態 |
|---------|------|:----:|
| 四種 deltaType 邏輯（in / reserve / cancel / out）| 第 691–708 行 | ✅ |
| 應用層約束檢查（DB 寫入前先算、先擋）| 第 711–715 行 | ✅ |
| Transaction 原子性（業務寫入 + audit 記錄同生共死）| sync.route.ts 第 117 行 | ✅ |
| DB 層 CHECK 約束（第二道防線）| inventory_items.schema.ts | ✅ |

### 雙重保護設計

```
應用層（先）：
  if (newOnHand < 0 || newReserved < 0 || newReserved > newOnHand)
    → INSUFFICIENT_STOCK + server_state（格式統一，前端可直接處理）

DB 層（兜底）：
  CHECK (quantity_on_hand >= 0)
  CHECK (quantity_reserved <= quantity_on_hand)
```

**刻意不依賴 DB CHECK 作為主要錯誤回應**：DB CHECK 觸發時只拋出 PG 原生錯誤，難以包裝成同步協定 v1.6 §6 標準格式。應用層先攔，DB CHECK 只作安全兜底。

### curl 實機驗收結果（4/4 通過）

**測試環境**：productId=3（TST-I7-001），seed：onHand=10, reserved=0, minStockLevel=2

| # | 測試 | 預期 | 結果 |
|---|------|------|:----:|
| 1 | POST /push `reserve` amount=3 | succeeded，quantityReserved: 0→3 | ✅ |
| 2 | GET /pull `inventory_delta` | inventoryItems 含正確 quantityReserved=3 + minStockLevel=2 | ✅ |
| 3 | POST /push `reserve` amount=8（超限，3+8>10）| INSUFFICIENT_STOCK + server_state（含現況快照）| ✅ |
| 4 | POST /push `cancel` amount=3 | succeeded，quantityReserved: 3→0 | ✅ |

**INSUFFICIENT_STOCK 回傳格式確認**（驗收 3 完整輸出）：
```json
{
  "code": "INSUFFICIENT_STOCK",
  "message": "庫存不足：onHand=10, reserved=3, delta=reserve(8)。",
  "server_state": {
    "entityType": "inventory_delta",
    "quantityOnHand": 10,
    "quantityReserved": 3,
    "minStockLevel": 2
  }
}
```

---

## 下次待辦（W6–W8 SCM 續）

| 優先 | Issue | 任務 |
|------|-------|------|
| 🔴 高 | curl 驗收 | POST /push reserve + INSUFFICIENT_STOCK → pullData()（需後端在線）|
| 🔴 高 | #10 | 出貨 UI（倉管 type: out）+ SalesOrderListScreen 出貨按鈕 |
| 🟡 中 | #11 | 庫存列表 UI（InventoryListScreen）+ 入庫 UI（type: in）|
| 🟡 中 | #12 | 併發衝突模擬測試（Race Condition）|
| 🟢 低 | #13 | 離線建單 → 同步 → 庫存更新 走路測試（需 Android 工具鏈）|
| ⬜ | — | Android 工具鏈安裝（W6 走路測試前必須就緒）|
